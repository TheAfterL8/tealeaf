{
  "name": "tealeaf-extension",
  "version": "0.1.0",
  "private": true,
  "description": "Chrome MV3 extension that surfaces C2PA content provenance on every image.",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "dev": "node esbuild.config.mjs --watch",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@contentauth/c2pa-wasm": "^0.9.0"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.270",
    "esbuild": "^0.23.0",
    "typescript": "^5.4.0"
  }
}
